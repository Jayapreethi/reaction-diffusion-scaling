# Fisher-KPP GPU/CPU Benchmark Suite
# Core dependencies for solver and benchmarking

torch>=2.0.0          # Deep learning framework with CUDA support
numpy>=1.20           # Numerical computing
matplotlib>=3.3       # Visualization
pytest>=7.0           # Unit testing framework
PyYAML>=6.0           # Configuration file parsing (required for pipeline)

# Optional but recommended
jupyter>=1.0          # Interactive notebooks
pandas>=1.3           # Data analysis (for result aggregation)
